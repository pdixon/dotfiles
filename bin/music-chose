#!/bin/zsh

chosen=$(mpc list album | dmenu -i -h 22 -nb "#3f3f3f" -nf "#cc9393" -sb "#3f3f3f" -sf "#f0dfaf" -fn "Source Sans Pro-9")

if [ "$chosen" != "" ]; then
    mpc clear
    mpc search album "$chosen" | mpc add
    mpc play
fi
