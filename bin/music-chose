#!/bin/bash

chosen=$(mpc list album | bemenu -i -p 'Album:' --fn 'Source Code Pro 10.0' --tf '#7aa6da' --hf '#7aa6da')

if [ "$chosen" != "" ]; then
    mpc clear
    mpc search album "$chosen" | mpc add
    mpc play
fi
