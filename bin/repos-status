#! /bin/bash

for repo in $(find ./ -maxdepth 1 -type d)
do
    if [ -d $repo/.hg ]
    then
        echo REPO: $repo
        cd ./$repo
        hg st
        hg outgoing -q --color=auto --template '{rev}:{node|short} {desc|firstline}\n'
        cd ..
    fi
done
