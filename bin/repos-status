#! /bin/bash

for repo in $(find . -type d -depth 1)
do
    if [ -d $repo/.hg ]
    then
        echo REPO: $repo
        cd ./$repo
        hg st
        hg outgoing -q --color=auto --template '{rev}:{node|short} {desc|firstline}\n'
        cd ..
    fi
done
