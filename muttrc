set from = "phil@dixon.gen.nz"
set realname = "Phillip Dixon"

set imap_user = "phil@dixon.gen.nz"

set folder = "imaps://imap.gmail.com:993/"
set spoolfile = "+INBOX"
set postponed ="+[Gmail]/Drafts"
# set trash = ""

set imap_check_subscribed

set header_cache =~/.mutt/cache/headers
set message_cachedir =~/.mutt/cache/bodies
set certificate_file =~/.mutt/certificates

set smtp_url = "smtp://phil@dixon.gen.nz@smtp.gmail.com:587/"

bind editor <space> noop

# mutt sidebar settings
set sidebar_delim = '|'
set sidebar_width=30
set sidebar_visible=yes
color sidebar_new green default


bind index,pager \Cp sidebar-prev
bind index,pager \Cn sidebar-next
bind index,pager \Co sidebar-open
macro index,pager \Ci '<enter-command>toggle sidebar_visible<enter>'

set move = no  #Stop asking to "move read messages to mbox"!

unset imap_passive
set imap_keepalive = 300

set print_command = "muttprint %s"

set edit_headers
set editor = "$EDITOR -nw %s"

source mutt-colors-solarized-light-16.muttrc